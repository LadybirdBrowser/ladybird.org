<section class="hero-landing">
  <div class="container">
    <h1>A truly independent<br />web browser</h1>
    <p class="hero-desc">
      Ladybird is a new browser engine built from scratch. Not a fork. No user
      monetization. Backed by a <a href="/organization/">501(c)(3) non-profit</a
      >.
    </p>
    <div class="hero-meta">
      <span><strong>Status:</strong> In active development</span>
      <span><strong>Target:</strong> Alpha 2026 (Linux & macOS)</span>
    </div>
    <div class="hero-cta">
      <a href="/#contribute" class="btn">Get involved</a>
      <a href="/#support" class="btn btn--outline">Support us</a>
    </div>
  </div>
</section>

<style>
  .hero-landing {
    padding: 80px 0 64px;
    border-bottom: 1px solid var(--border);
  }

  .hero-landing h1 {
    font-size: 48px;
    font-weight: var(--weight-light);
    line-height: 1.15;
    margin-bottom: 20px;
  }

  .hero-desc {
    font-size: 18px;
    max-width: 520px;
    color: var(--text-secondary);
    margin-bottom: 20px;
  }

  @media (max-width: 640px) {
    .hero-landing {
      padding: 48px 0 40px;
    }

    .hero-landing h1 {
      font-size: 32px;
      line-height: 1.1;
      margin-bottom: 16px;
    }

    .hero-desc {
      font-size: 16px;
    }

    .hero-meta {
      margin-bottom: 24px;
    }

    .hero-cta {
      flex-direction: column;
      gap: 10px;
    }

    .hero-cta .btn {
      text-align: center;
    }
  }

  .hero-desc a {
    color: var(--color-accent);
  }

  .hero-meta {
    display: flex;
    flex-direction: column;
    gap: 4px;
    font-size: 15px;
    color: var(--text-muted);
    margin-bottom: 32px;
  }

  .hero-meta strong {
    color: var(--text-primary);
  }

  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
</style>
